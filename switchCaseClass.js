const age = 20;

switch(true) {
    case age >= 18:
        console.log("You are eligible");
        break;
    // case age < 18:
    //     console.log("You are not eligible")
    //     break
    default:
        console.log("You are not eligible")
}